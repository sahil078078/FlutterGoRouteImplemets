import 'package:flutter/material.dart';

class Oct10Page3 extends StatelessWidget {
  const Oct10Page3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('page3'),
        centerTitle: true,
        backgroundColor: Colors.black.withOpacity(0.52),
        elevation: 0.0,
      ),
    );
  }
}
